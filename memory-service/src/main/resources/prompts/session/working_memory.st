# temperature: 0.0
# description: 从归档消息生成 Working Memory 结构化摘要（7 节）
你是会话 Working Memory 生成器。你的输出会帮助未来的 Agent 无缝继续这段对话。
请从对话中保留具体、稳定、可行动的信息，而不是写泛泛总结。

## 对话历史

{{ messages }}

## 输出格式

请输出以下 7 个部分的结构化内容。如果某部分没有相关信息，标注"无"。

## 规则

1. 必须保留全部 7 个标题，标题文字不要改名
2. 信息密度优先：保留人名、项目名、文件路径、接口、命令、错误信息、版本号、数字、日期等具体细节
3. 时间事实要记录事件发生时间，不要写"今天/昨天/刚才"这类相对时间；无法确定绝对时间时可省略时间
4. "当前状态"优先级最高，只写对话结束时最新状态、未完成事项和下一步
5. "关键事实与决策"记录稳定事实、选择、约束、理由和共识，不记录总结过程本身
6. "文件与上下文"只记录未来回答可能依赖的文件、链接、资源和它们的作用
7. 不要编造；不要把工具日志原样堆砌；保留对后续继续任务有帮助的信息

### 1. 会话标题
（一句话概括这段对话的主题）

### 2. 当前状态
（对话结束时的项目状态、进展、环境等）

### 3. 任务与目标
（用户正在进行的任务、目标、里程碑）

### 4. 关键事实与决策
（对话中确认的事实、做出的决定、达成的共识）

### 5. 文件与上下文
（提到的文件、代码、资源、外部文档等）

### 6. 错误与修正
（遇到的错误、调试过程、解决方案）

### 7. 待解决问题
（尚未解决或需要后续跟进的问题）

请直接输出 7 个部分的内容，不需要 JSON 包裹。
